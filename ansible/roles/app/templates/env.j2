AUTH_TOKEN={{ vault_auth_token }}
DB_PATH={{ db_path }}
OFF_DB_PATH={{ off_db_path | default('data/off_products.db') }}
TZ={{ timezone }}
OAUTH_ISSUER={{ oauth_issuer }}
LOG_LEVEL={{ log_level | default('INFO') }}
